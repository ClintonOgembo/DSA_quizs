class Solution {
    /**
     * @param {string[]} strs
     * @returns {string}
     */
    encode(strs) {
        const newStr = strs.join('')
        console.log(newStr);
    }

    /**
     * @param {string} str
     * @returns {string[]}
     */
    decode(str) { }
}
